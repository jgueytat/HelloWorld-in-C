{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "target": "./build/hello",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}